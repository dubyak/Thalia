/**
 * StatusBar previously rendered fake phone chrome (time, wifi, battery).
 * Removed because users access the app in their real mobile browser,
 * which already shows these indicators.
 */
export function StatusBar(_props: { dark?: boolean }) {
  return null
}
